package med.voll.api.DTO;
/*
import med.voll.api.model.Paciente;
//record para limitar los datos que queremos listar
public record DatosListadoPaciente(String nombre, String especialidad,String documento,String email) {
	public DatosListadoPaciente (Paciente paciente) {
		this(medico.getNombre(), medico.getEspecialidad().toString(), medico.getDocumento(), medico.getEmail());
	}
}*/
